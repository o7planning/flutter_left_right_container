name: flutter_left_right_container
description: "Left Right Container"
version: 0.9.0
resolution: workspace
homepage: https://o7planning.github.io

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_artist_core: ^0.9.1
  flutter_artist_styles: ^0.9.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


flutter:

# -------------------------------------------------------
# dependency_overrides:
# -------------------------------------------------------
