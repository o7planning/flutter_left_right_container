part of '../../flutter_left_right_container.dart';

/// Defines the anchor targets specifying which side container maintains
/// a fixed, un-clamped dimension constraint layout footprint.
enum FixedSide {
  /// The left or starting layout pane holds the static explicit width boundary metrics.
  start,

  /// The right or ending layout pane holds the static explicit width boundary metrics.
  end,
}
